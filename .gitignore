node_modules
dist
.DS_Store
*.log
.vite
*.tsbuildinfo
.turbo
# A pattern with a slash anchors to this directory, so these are written with
# **/ to reach apps/demo/recordings too. Recordings are the user's own audio;
# whisper models are hundreds of megabytes.
**/recordings/*/
!**/recordings/.gitkeep
models
