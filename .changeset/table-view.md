---
"@open-recording/core": patch
---

See recordings as a table as well as cards.

Cards are for recognising one recording; a table is for comparing many — length, size, tags, and whether each one has a transcript, read down a column instead of across a page. The choice is remembered.
